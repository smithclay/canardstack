# AWS ECS/Fargate

This directory contains the CloudFormation template for the canardstack
ECS/Fargate deployment.

The operator guide lives in the site source:

- [AWS ECS/Fargate deployment docs](../../../site/src/content/docs/deployment/aws-ecs-fargate.md)

Keep deployment instructions there so the rendered site and repository docs do
not drift apart.
