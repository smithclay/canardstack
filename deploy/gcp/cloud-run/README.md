# GCP Cloud Run

This directory contains the Terraform module for the canardstack Cloud Run
deployment.

The operator guide lives in the site source:

- [GCP Cloud Run deployment docs](../../../site/src/content/docs/deployment/gcp-cloud-run.md)

Keep deployment instructions there so the rendered site and repository docs do
not drift apart.
