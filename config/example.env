# Env vars override config.toml. Prefer config/example.toml for the primary
# operator config file, then use env vars for deployment-specific overrides.
# CANARDSTACK_CONFIG=/etc/canardstack/config.toml
CANARDSTACK_BIND=127.0.0.1:4318
# Optional in-binary TLS for the public serve endpoint. This requires a build
# with `--features tls`. File mode follows common observability config shape:
# cert_file/key_file are PEM files, typically managed by ACME/cert-manager.
# CANARDSTACK_TLS_ENABLED=true
# CANARDSTACK_TLS_MODE=file
# CANARDSTACK_TLS_CERT_FILE=/etc/canardstack/tls/fullchain.pem
# CANARDSTACK_TLS_KEY_FILE=/etc/canardstack/tls/privkey.pem
# CANARDSTACK_TLS_BACKEND_BIND=127.0.0.1:4319
#
# Explicit single-binary dev/service-to-service mode. This encrypts traffic but
# clients must trust the generated cert or skip verification.
# CANARDSTACK_TLS_MODE=ephemeral_self_signed
CANARDSTACK_API_KEY=dev-canardstack-key
CANARDSTACK_ADMIN_API_KEY=dev-canardstack-admin-key
CANARDSTACK_DATA_DIR=.canardstack
# Optional. Docker sets this to the baked extension cache.
# CANARDSTACK_DUCKDB_EXTENSION_DIR=/usr/local/lib/duckdb/extensions

# For a DuckLake PostgreSQL metadata catalog, create the database first and set:
# CANARDSTACK_POSTGRES_DSN=dbname=ducklake_catalog host=localhost user=postgres password=postgres
#
# Optional local DuckDB-backed DuckLake metadata catalog file override.
# CANARDSTACK_DUCKLAKE_CATALOG_PATH=/var/lib/canardstack/catalog/canardstack.ducklake
#
# Optional DuckLake DATA_PATH override for object storage. Keep unset for local
# data files under CANARDSTACK_DATA_DIR/storage. canardstack creates a DuckDB
# credential_chain secret for s3://, s3a://, gcs://, and gs:// paths. For S3 the
# chain resolves the ambient role (ECS task / EC2 instance / env). For GCS, core
# DuckDB only speaks the S3-compatible interop API, so supply HMAC interop keys
# via the standard AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY env vars.
# CANARDSTACK_DUCKLAKE_DATA_PATH=s3://canardstack-data/prod/
#
# Required when CANARDSTACK_DUCKLAKE_ATTACH_URI points at a Quack catalog.
# CANARDSTACK_DUCKLAKE_QUACK_TOKEN=replace-with-catalog-token
#
# Optional local/single-node live catalog endpoint. This starts an in-process
# loopback Quack catalog endpoint and makes canardstack attach through it via
# ducklake:quack:<listen>, so DuckDB clients can inspect the local DuckLake
# while canardstack keeps running. The endpoint is loopback-only and always
# plaintext; CANARDSTACK_CATALOG_TLS* applies only to the separate
# serve-catalog role and has no effect here.
# CANARDSTACK_LOCAL_CATALOG_ENABLED=true
# CANARDSTACK_LOCAL_CATALOG_LISTEN=127.0.0.1:9494
#
# For a read-write MotherDuck-hosted DuckLake, set the attach URI and provide a
# MotherDuck token. Keep CANARDSTACK_POSTGRES_DSN unset in this mode.
# CANARDSTACK_DUCKLAKE_ATTACH_URI=md:test-ducklake
# motherduck_token=<your-motherduck-token>

# Uses DuckDB's official DuckLake extension SQL path. Startup fails if the
# extension/catalog cannot attach; there is no non-DuckLake ingest fallback.
CANARDSTACK_DUCKLAKE_MAINTENANCE_ENABLED=true
CANARDSTACK_DUCKLAKE_DATA_INLINING_ROW_LIMIT=10
# Defaults to CANARDSTACK_RETENTION_DAYS when unset.
# CANARDSTACK_DUCKLAKE_EXPIRE_OLDER_THAN_DAYS=14
CANARDSTACK_DUCKLAKE_DELETE_OLDER_THAN_SECS=86400

CANARDSTACK_MAX_BODY_BYTES=8388608
# Optional hard RSS admission limit; unset/empty disables runtime RSS checks.
CANARDSTACK_PROCESS_MEMORY_LIMIT_BYTES=
CANARDSTACK_ACCEPT_LATE_SECS=86400
CANARDSTACK_ACCEPT_FUTURE_SECS=600
CANARDSTACK_DUCKDB_MEMORY_LIMIT=1GiB
CANARDSTACK_ARROW_WRITE_BUFFER_TARGET_BYTES=67108864
CANARDSTACK_ARROW_WRITE_BUFFER_MAX_AGE_SECS=10
# Per raw-spool request kind; logs, traces, and metrics can use about 3x this in aggregate.
CANARDSTACK_RAW_SPOOL_CAPACITY_BYTES=1073741824
CANARDSTACK_RAW_SPOOL_GROUP_COMMIT_MS=1
CANARDSTACK_QUERY_CONCURRENCY=4
CANARDSTACK_QUERY_TIMEOUT_SECS=15
CANARDSTACK_QUERY_MEMORY_LIMIT=1GiB
CANARDSTACK_QUERY_ADMISSION_WAIT_MS=1000
CANARDSTACK_SEAL_ADMISSION_CAPACITY=1
CANARDSTACK_CHEAP_QUERY_ADMISSION_CAPACITY=1
CANARDSTACK_HEAVY_QUERY_DEGRADED_CAPACITY=1
CANARDSTACK_FRESHNESS_BUDGET_SLA_SECS=15
CANARDSTACK_RETENTION_DAYS=14
CANARDSTACK_MAX_CONNECTIONS=1024
CANARDSTACK_SOCKET_READ_TIMEOUT_SECS=30
CANARDSTACK_SOCKET_WRITE_TIMEOUT_SECS=30

# Background maintenance. The serve command spawns one thread that runs:
#   - single seal driver (Arrow write buffer -> DuckDB Arrow append -> DuckLake commit)
#   - discovery metadata refresh
#   - operator metrics snapshot
#   - retention, followed by DuckLake CHECKPOINT when DuckLake maintenance is enabled
# Use /api/admin/maintenance/checkpoint/run to trigger DuckLake physical
# maintenance without applying logical telemetry retention deletes.
CANARDSTACK_SCHEDULER_ENABLED=true
CANARDSTACK_MAINTENANCE_INTERVAL_SECS=30

# Persist a snapshot of operator metrics into the metric_gauge / metric_sum
# storage tables (queryable via the compat path). Off by default to avoid the
# extra write load and the canardstack_operator_metrics rows; /metrics still
# serves the live surface.
# CANARDSTACK_OPERATOR_METRICS_TO_STORAGE=false
#
# Fine-grained spool phase micro-timings are a compile-time opt-in, not an env
# knob: build with `cargo build --features detailed-metrics` to emit them. The
# coarse phases are always on.
